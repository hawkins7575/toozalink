# 커스텀 도메인 성능 최적화 리다이렉트 설정
# www를 non-www로 리다이렉트 (성능 최적화)
https://www.tooza.shop/* https://tooza.shop/:splat 301!

# HTTP를 HTTPS로 리다이렉트 (보안 및 성능)
http://tooza.shop/* https://tooza.shop/:splat 301!
http://www.tooza.shop/* https://tooza.shop/:splat 301!

# 정적 자산 최적화 리다이렉트
/static/* /static/:splat 200
  Cache-Control: public, max-age=31536000, immutable
  
# API 라우팅 최적화
/api/* /.netlify/functions/:splat 200
  Cache-Control: no-cache, no-store, must-revalidate

# SPA 라우팅 지원 (최적화된 헤더 포함)
/* /index.html 200
  Cache-Control: no-cache, no-store, must-revalidate
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff